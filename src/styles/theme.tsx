'use client'

import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#23395d",
        },
        secondary: {
            main: "#F34C50",
        },
    },
    shape: {
        borderRadius: 14,
    }
});


export default theme;